<?php
// tests/Pest.php
// Require Composer autoloader so your PSR-4 src/ autoloading works in tests.
require_once __DIR__ . '/../vendor/autoload.php';

// Optional: global test helpers / hooks can go here.
// Example: a global beforeEach hook (delete or adapt as needed)
uses()->beforeEach(function () {
    // global setup for each test (if needed)
});
